[project]
name = "ws-root"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["ws-api", "ws-app"]

[tool.uv.workspace]
members = ["apps/*"]

[tool.uv.sources]
ws-api = { workspace = true }
ws-app = { workspace = true }
